package_update: true
packages:
  - git
  - curl

runcmd:
  - mkdir -p /home/ubuntu/tracker-infra
  - chown -R ubuntu:ubuntu /home/ubuntu/tracker-infra

# multipass launch --name tracker-test --cloud-init multipass-init.yaml
# multipass mount . tracker-test:/home/ubuntu/tracker-infra
# multipass shell tracker-test